const config = {
  coverageDirectory: '../../coverage/workflow-logger',
  displayName: 'workflow-logger',
  globals: {},
  cache: false,
  testEnvironment: 'node',
  testMatch: ['**/?(*.)+(spec|test).js'],
  transform: {},
};

export default config;
