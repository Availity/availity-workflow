const config = {
  coverageDirectory: '../../coverage/workflow-upgrade',
  displayName: 'workflow-upgrade',
  globals: {},
  cache: false,
  testEnvironment: 'node',
  testMatch: ['**/?(*.)+(spec|test).js'],
  transform: {},
};

export default config;
